import React, { Component } from 'react'
import { CommentButton, ButtonText } from './styledComponents'

function PostCommentButton() {
  return (
    <CommentButton>
      <ButtonText>Add comment</ButtonText>
    </CommentButton>
  )
}

export default PostCommentButton
